import React from "react";
import "./HomeButton.scss";

const HomeButton = (props) => {
  return (
    <button className="home-button" onClick={props.onClick}>
      return
    </button>
  );
};

export default HomeButton;
